// proceso master 

#include "master.h"

/*
/*  -w ancho tablero
/* -h altura tablero
/* -d milisegundos que espera el máster cada vez que se imprime el estado
/* -t timeout en segundos para recibir solicitudes de movimientos válidos
/* -s semilla utilizada para la generación del tablero
/* -v ruta del binario de la vista
/* -p ruta de los binarios de los jugadores. Mínimo 1, Máximo 9
*/

// int parse_args(int argc, char *argv[], const char *optstring){
//     int opt; 
//     while((opt = getopt(argc, argv, "p:w:h:d:t")) != 1){
//         switch
//     }
// }